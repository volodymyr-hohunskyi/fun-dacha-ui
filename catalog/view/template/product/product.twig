{{ header }}
{% if schema_product %}
<script type="application/ld+json">
{{ schema_product }}
</script>
{% endif %}
{% if schema_breadcrumb %}
<script type="application/ld+json">
{{ schema_breadcrumb }}
</script>
{% endif %}
{% if schema_faq %}
<script type="application/ld+json">
{{ schema_faq }}
</script>
{% endif %}
{% if og_tags %}
{% for key, value in og_tags %}
<meta property="{{ key }}" content="{{ value }}" />
{% endfor %}
{% endif %}
{% if twitter_tags %}
{% for key, value in twitter_tags %}
<meta name="{{ key }}" content="{{ value }}" />
{% endfor %}
{% endif %}
<div id="product-info" class="container">
  <nav class="category-breadcrumb" aria-label="breadcrumb">
    <ol class="breadcrumb">
      {% for breadcrumb in breadcrumbs %}
        <li class="breadcrumb-item{% if loop.last %} active{% endif %}">
          {% if loop.last %}{{ breadcrumb.text }}{% else %}<a href="{{ breadcrumb.href }}">{{ breadcrumb.text }}</a>{% endif %}
        </li>
      {% endfor %}
    </ol>
  </nav>
  <div class="row">{{ column_left }}
    <div id="content" class="col">
      {{ content_top }}
      <div class="row mb-3">
        {% if thumb or images %}
          <div class="col-sm">
            <div class="image magnific-popup">
              {% if thumb %}
                <a href="{{ popup }}" title="{{ heading_title }}"><img src="{{ thumb }}" title="{{ heading_title }}" alt="{{ image_alt|default(heading_title) }}" class="img-thumbnail mb-3" fetchpriority="high" decoding="async"/></a>
              {% endif %}
              {% if images %}
                <div>
                  {% for image in images %}
                    <a href="{{ image.popup }}" title="{{ heading_title }}"><img src="{{ image.thumb }}" title="{{ heading_title }}" alt="{{ image_alt|default(heading_title) }} — фото {{ loop.index }}" loading="lazy" decoding="async" class="img-thumbnail"/></a>&nbsp;
                  {% endfor %}
                </div>
              {% endif %}
            </div>
          </div>
        {% endif %}
        <div class="col-sm">
          <h1>{{ heading_title }}</h1>
          <ul class="list-unstyled">
            {% if manufacturer %}
              <li>{{ text_manufacturer }} <a href="{{ manufacturers }}">{{ manufacturer }}</a></li>
            {% endif %}
            {% if reward %}
              <li>{{ text_reward }} {{ reward }}</li>
            {% endif %}
            <li>{{ text_stock }} {{ stock }}</li>
          </ul>
          {% if quick_specs is defined and quick_specs and not is_bundle %}
            <div class="pdp-quick-specs">
              {% for spec in quick_specs %}
                <span class="pdp-quick-specs__tag">
                  <i class="fa-solid {{ spec.icon }}"></i> {{ spec.value }}
                </span>
              {% endfor %}
            </div>
          {% endif %}
          {% if review_status %}
            <div class="rating pdp-rating">
              <p>
                {% for i in 1..5 %}
                  {% if rating < i %}
                    <i class="fa-regular fa-star"></i>
                  {% else %}
                    <i class="fa-solid fa-star"></i>
                  {% endif %}
                {% endfor %}
                <a href="#" onclick="$('a[href=\'#tab-review\']').tab('show'); return false;">{{ text_reviews }}</a> / <a href="#" onclick="$('a[href=\'#tab-review\']').tab('show'); setTimeout(function(){ document.getElementById('pdp-write-review') &amp;&amp; document.getElementById('pdp-write-review').scrollIntoView({behavior:'smooth', block:'start'}); }, 150); return false;">{{ text_write }}</a></p>
            </div>
          {% endif %}
          {% if price %}
            {% if pdp_pack_options %}
              {% set first_pack = pdp_pack_options[0] %}
            <ul class="list-unstyled product-price-block">
              <li class="list-unstyled border-0 p-0 mb-0">
                <ul class="list-unstyled mb-0" id="pdp-main-price">
                  {% if first_pack.pack_line_was_formatted %}
                    <li class="mb-1 product-price-block__row product-price-block__row--sale">
                      <span class="product-price-sale-label">{{ text_special_offer }}</span>
                      <span class="product-price-sale price-display__sale">{{ first_pack.pack_total_formatted }}</span>
                    </li>
                    <li class="mb-0 product-price-block__row product-price-block__row--was"><span class="product-price-was price-display__was">{{ first_pack.pack_line_was_formatted }}</span></li>
                  {% else %}
                    <li><h2 class="mb-0"><span class="product-price-regular">{{ first_pack.pack_total_formatted }}</span></h2></li>
                  {% endif %}
                </ul>
              </li>
              {% if points %}
                <li>{{ text_points }} {{ points }}</li>
              {% endif %}
            </ul>
            {% else %}
            <ul class="list-unstyled product-price-block">
              {% if not special %}
                <li><h2 class="mb-0"><span class="product-price-regular">{{ price }}</span></h2></li>
              {% else %}
                <li class="mb-1 product-price-block__row product-price-block__row--sale">
                  <span class="product-price-sale-label">{{ text_special_offer }}</span>
                  <span class="product-price-sale price-display__sale">{{ special }}</span>
                </li>
                <li class="mb-0 product-price-block__row product-price-block__row--was"><span class="product-price-was price-display__was">{{ price }}</span></li>
              {% endif %}
              {% if points %}
                <li>{{ text_points }} {{ points }}</li>
              {% endif %}
            </ul>
            {% endif %}
            {% if not is_bundle %}
            <section class="pdp-pack-under-price mt-3 mb-3" aria-labelledby="pdp-heading-pack-inline">
              <h2 id="pdp-heading-pack-inline" class="pdp-detail-section__title">{{ text_pdp_size_heading }}</h2>
              <div class="pdp-detail-section__body">
                {% if pdp_pack_options %}
                  <div class="pdp-pack-choice-grid" role="group" aria-label="{{ text_pdp_size_heading }}">
                    {% for box in pdp_pack_options %}
                      <button type="button" class="pdp-pack-choice-box{% if loop.first %} pdp-pack-choice-box--active{% endif %}" data-pov-id="{{ box.product_option_value_id }}" data-pdp-sale="{{ box.pack_total_formatted|e('html_attr') }}" data-pdp-was="{{ box.pack_line_was_formatted|e('html_attr') }}" aria-label="{{ box.pack_label }} {{ box.pack_total_formatted }} {{ box.pack_unit_formatted }}">
                        <span class="pdp-pack-choice-box__label d-block fw-semibold">{{ box.pack_label }}</span>
                        <span class="pdp-pack-choice-box__total d-block">{{ box.pack_total_formatted }}</span>
                        <span class="pdp-pack-choice-box__unit d-block text-muted small">{{ box.pack_unit_formatted }}</span>
                        {% if box.pack_savings_percent is not null %}
                          <span class="pdp-pack-choice-box__save d-block small text-success">−{{ box.pack_savings_percent }}%</span>
                        {% endif %}
                        {% if pdp_pack_unit_label %}
                          <span class="pdp-pack-choice-box__footer d-block small">{{ pdp_pack_unit_label }}</span>
                        {% endif %}
                      </button>
                    {% endfor %}
                  </div>
                  <input type="hidden" form="form-product" name="option[{{ pdp_pack_product_option_id }}]" id="pdp-pack-option-input" value="{{ pdp_pack_default_product_option_value_id }}" />
                {% else %}
                  <p class="mb-0">{{ pdp_size_intro ?: text_pdp_size_intro }}</p>
                  {% if text_pdp_size_future %}
                    <p class="mb-0 mt-2 text-muted small">{{ text_pdp_size_future }}</p>
                  {% endif %}
                {% endif %}
              </div>
            </section>
            {% endif %}
          {% endif %}
          <div id="product">
            <form id="form-product">
              {% if options %}
                <hr>
                <h3>{{ text_option }}</h3>
                <div>
                  {% for option in options %}

                    {% if option.type == 'select' %}
                      <div class="mb-3{% if option.required %} required{% endif %}">
                        <label for="input-option-{{ option.product_option_id }}" class="form-label">{{ option.name }}</label>
                        <select name="option[{{ option.product_option_id }}]" id="input-option-{{ option.product_option_id }}" class="form-select">
                          <option value="">{{ text_select }}</option>
                          {% for option_value in option.product_option_value %}
                            <option value="{{ option_value.product_option_value_id }}">{% if option_value.pack_display %}{{ option_value.pack_display.select_label }}{% else %}{{ option_value.name }}{% if option_value.price %}
                                ({{ option_value.price_prefix }}{{ option_value.price }})
                              {% endif %}{% endif %}</option>
                          {% endfor %}
                        </select>
                        <div id="error-option-{{ option.product_option_id }}" class="invalid-feedback"></div>
                      </div>
                    {% endif %}

                    {% if option.type == 'radio' %}
                      <div class="mb-3{% if option.required %} required{% endif %}">
                        <label class="form-label">{{ option.name }}</label>
                        <div id="input-option-{{ option.product_option_id }}">
                          {% for option_value in option.product_option_value %}
                            <div class="form-check">
                              <input type="radio" name="option[{{ option.product_option_id }}]" value="{{ option_value.product_option_value_id }}" id="input-option-value-{{ option_value.product_option_value_id }}" class="form-check-input"/>
                              <label for="input-option-value-{{ option_value.product_option_value_id }}" class="form-check-label">
                                {% if option_value.image %}
                                  <img src="{{ option_value.image }}" alt="{{ option_value.pack_display ? option_value.pack_display.label : option_value.name }}" class="img-thumbnail"/>{% endif %}
                                {% if option_value.pack_display %}
                                  <span class="d-block fw-semibold">{{ option_value.pack_display.label }}</span>
                                  <span class="d-block">{{ option_value.pack_display.total_formatted }}</span>
                                  <span class="d-block text-muted small">{{ option_value.pack_display.unit_formatted }}</span>
                                  {% if option_value.pack_display.savings_percent is not null %}
                                    <span class="d-block small text-success">−{{ option_value.pack_display.savings_percent }}%</span>
                                  {% endif %}
                                {% else %}
                                  {{ option_value.name }}
                                {% if option_value.price %}
                                  ({{ option_value.price_prefix }}{{ option_value.price }})
                                {% endif %}
                                {% endif %}</label>
                            </div>
                          {% endfor %}
                        </div>
                        <div id="error-option-{{ option.product_option_id }}" class="invalid-feedback"></div>
                      </div>
                    {% endif %}

                    {% if option.type == 'checkbox' %}
                      <div class="mb-3{% if option.required %} required{% endif %}">
                        <label class="form-label">{{ option.name }}</label>
                        <div id="input-option-{{ option.product_option_id }}">
                          {% for option_value in option.product_option_value %}
                            <div class="form-check">
                              <input type="checkbox" name="option[{{ option.product_option_id }}][]" value="{{ option_value.product_option_value_id }}" id="input-option-value-{{ option_value.product_option_value_id }}" class="form-check-input"/> 
							                <label for="input-option-value-{{ option_value.product_option_value_id }}" class="form-check-label">
                                {% if option_value.image %}
                                  <img src="{{ option_value.image }}" alt="{{ option_value.pack_display ? option_value.pack_display.label : option_value.name }}" class="img-thumbnail"/>{% endif %}
                                {% if option_value.pack_display %}
                                  <span class="d-block fw-semibold">{{ option_value.pack_display.label }}</span>
                                  <span class="d-block">{{ option_value.pack_display.total_formatted }}</span>
                                  <span class="d-block text-muted small">{{ option_value.pack_display.unit_formatted }}</span>
                                  {% if option_value.pack_display.savings_percent is not null %}
                                    <span class="d-block small text-success">−{{ option_value.pack_display.savings_percent }}%</span>
                                  {% endif %}
                                {% else %}
                                  {{ option_value.name }}
                                {% if option_value.price %}
                                  ({{ option_value.price_prefix }}{{ option_value.price }})
                                {% endif %}
                                {% endif %}</label>
                            </div>
                          {% endfor %}
                        </div>
                        <div id="error-option-{{ option.product_option_id }}" class="invalid-feedback"></div>
                      </div>
                    {% endif %}

                    {% if option.type == 'text' %}
                      <div class="mb-3{% if option.required %} required{% endif %}">
                        <label for="input-option-{{ option.product_option_id }}" class="form-label">{{ option.name }}</label> <input type="text" name="option[{{ option.product_option_id }}]" value="{{ option.value }}" placeholder="{{ option.name }}" id="input-option-{{ option.product_option_id }}" class="form-control"/>
                        <div id="error-option-{{ option.product_option_id }}" class="invalid-feedback"></div>
                      </div>
                    {% endif %}

                    {% if option.type == 'textarea' %}
                      <div class="mb-3{% if option.required %} required{% endif %}">
                        <label for="input-option-{{ option.product_option_id }}" class="form-label">{{ option.name }}</label> <textarea name="option[{{ option.product_option_id }}]" rows="5" placeholder="{{ option.name }}" id="input-option-{{ option.product_option_id }}" class="form-control">{{ option.value }}</textarea>
                        <div id="error-option-{{ option.product_option_id }}" class="invalid-feedback"></div>
                      </div>
                    {% endif %}

                    {% if option.type == 'file' %}
                      <div class="mb-3{% if option.required %} required{% endif %}">
                        <label for="button-upload-{{ option.product_option_id }}" class="form-label">{{ option.name }}</label>
                        <div>
                          <button type="button" id="button-upload-{{ option.product_option_id }}" data-oc-toggle="upload" data-oc-url="{{ upload }}" data-oc-target="#input-option-{{ option.product_option_id }}" data-oc-size-max="{{ config_file_max_size }}" data-oc-size-error="{{ error_upload_size }}" class="btn btn-light btn-block"><i class="fa-solid fa-upload"></i> {{ button_upload }}</button>
                          <input type="hidden" name="option[{{ option.product_option_id }}]" value="" id="input-option-{{ option.product_option_id }}"/>
                        </div>
                        <div id="error-option-{{ option.product_option_id }}" class="invalid-feedback"></div>
                      </div>
                    {% endif %}

                    {% if option.type == 'date' %}
                      <div class="mb-3{% if option.required %} required{% endif %}">
                        <label for="input-option-{{ option.product_option_id }}" class="form-label">{{ option.name }}</label>
                        <input type="date" name="option[{{ option.product_option_id }}]" value="{{ option.value }}" id="input-option-{{ option.product_option_id }}" class="form-control"/>
                        <div id="error-option-{{ option.product_option_id }}" class="invalid-feedback"></div>
                      </div>
                    {% endif %}

                    {% if option.type == 'time' %}
                      <div class="mb-3{% if option.required %} required{% endif %}">
                        <label for="input-option-{{ option.product_option_id }}" class="form-label">{{ option.name }}</label>
                        <input type="time" name="option[{{ option.product_option_id }}]" value="{{ option.value }}" id="input-option-{{ option.product_option_id }}" class="form-control"/>
                        <div id="error-option-{{ option.product_option_id }}" class="invalid-feedback"></div>
                      </div>
                    {% endif %}

                    {% if option.type == 'datetime' %}
                      <div class="mb-3{% if option.required %} required{% endif %}">
                        <label for="input-option-{{ option.product_option_id }}" class="form-label">{{ option.name }}</label>
                        <input type="datetime-local" name="option[{{ option.product_option_id }}]" value="{{ option.value }}" id="input-option-{{ option.product_option_id }}" class="form-control"/>
                        <div id="error-option-{{ option.product_option_id }}" class="invalid-feedback"></div>
                      </div>
                    {% endif %}
                  {% endfor %}
                </div>
              {% endif %}

              {% if subscription_plans %}
                <hr/>
                <h3>{{ text_subscription }}</h3>
                <div class="mb-3 required">
                  <select name="subscription_plan_id" id="input-subscription" class="form-select">
                    <option value="">{{ text_select }}</option>
                    {% for subscription_plan in subscription_plans %}
                      <option value="{{ subscription_plan.subscription_plan_id }}">{{ subscription_plan.name }}</option>
                    {% endfor %}
                  </select>
                  {% for subscription_plan in subscription_plans %}
                    <div id="subscription-description-{{ subscription_plan.subscription_plan_id }}" class="form-text subscription d-none">{{ subscription_plan.description }}</div>
                  {% endfor %}
                  <div id="error-subscription" class="invalid-feedback"></div>
                </div>
              {% endif %}
{% if is_bundle %}
<section class="pdp-pack-under-price mt-3 mb-3">
  <div class="pdp-detail-section__body">
    <div class="d-flex flex-column gap-2 align-items-stretch pdp-qty-cart-row">
      <button type="submit"
              id="button-cart"
              class="btn btn-primary btn-lg text-white add-to-cart-btn">
        <i class="fa-solid fa-box-open"></i> {{ text_bundle_add }}
      </button>
    </div>
    <input type="hidden" name="quantity" value="1" />
    <input type="hidden" name="product_id" value="{{ product_id }}" />
  </div>
</section>
{% else %}
<section class="pdp-pack-under-price mt-3 mb-3" aria-labelledby="pdp-heading-quantity">
  <h2 id="pdp-heading-quantity" class="pdp-detail-section__title">{{ text_pdp_quantity_heading }}</h2>
  <div class="pdp-detail-section__body">
    <div class="d-flex flex-column gap-2 align-items-stretch pdp-qty-cart-row">

      <!-- Quantity -->
      <div class="input-group input-group-qty-compact quantity-group">
        <button type="button"
                class="btn btn-primary btn-qty-step text-white"
                onclick="changeQuantity(-1)">
          <i class="fa-solid fa-minus" aria-hidden="true"></i>
        </button>

        <input type="text"
               name="quantity"
               value="{{ minimum }}"
               id="input-quantity"
               class="form-control form-control-sm text-center quantity-input" />

        <button type="button"
                class="btn btn-primary btn-qty-step text-white"
                onclick="changeQuantity(1)">
          <i class="fa-solid fa-plus" aria-hidden="true"></i>
        </button>
      </div>

      <!-- Add to cart -->
      <button type="submit"
              id="button-cart"
              class="btn btn-primary btn-lg text-white add-to-cart-btn">
        {{ button_cart }}
      </button>

    </div>

    <input type="hidden" name="product_id" value="{{ product_id }}" />
    <div id="error-quantity" class="form-text"></div>
  </div>
</section>
{% endif %}

              <div class="pdp-trust-badges mt-3">
                {% if is_bundle %}
                <div class="pdp-trust-badge"><i class="fa-solid fa-box-open"></i> Все в одному наборі</div>
                {% endif %}
                <div class="pdp-trust-badge"><i class="fa-solid fa-truck-fast"></i> Нова Пошта 1-3 дні</div>
                <div class="pdp-trust-badge"><i class="fa-solid fa-money-bill-wave"></i> Оплата при отриманні</div>
                {% if not is_bundle %}
                <div class="pdp-trust-badge"><i class="fa-solid fa-circle-check"></i> Гарантія сходження</div>
                {% endif %}
              </div>

              {% if minimum > 1 %}
                <div class="alert alert-warning"><i class="fa-solid fa-circle-info"></i> {{ text_minimum }}</div>
              {% endif %}
            </form>
          </div>
        </div>
      </div>

      <div class="pdp-tabs mt-4">
        <ul class="nav nav-tabs" role="tablist">
          {% if description %}
            <li class="nav-item" role="presentation"><button class="nav-link active" data-bs-toggle="tab" data-bs-target="#tab-description" type="button" role="tab">Опис</button></li>
          {% endif %}
          {% if attribute_groups %}
            <li class="nav-item" role="presentation"><button class="nav-link{% if not description %} active{% endif %}" data-bs-toggle="tab" data-bs-target="#tab-specs" type="button" role="tab">Характеристики</button></li>
          {% endif %}
          <li class="nav-item" role="presentation"><button class="nav-link" data-bs-toggle="tab" data-bs-target="#tab-shipping" type="button" role="tab">Доставка</button></li>
          {% if review_status %}
            <li class="nav-item" role="presentation"><button class="nav-link" data-bs-toggle="tab" data-bs-target="#tab-review" type="button" role="tab">{{ tab_review }}</button></li>
          {% endif %}
        </ul>
        <div class="tab-content">
          {% if description %}
            <div id="tab-description" class="tab-pane fade show active" role="tabpanel">
              <div class="pdp-tab-body pdp-growing-text">{{ description }}</div>
            </div>
          {% endif %}
          {% if attribute_groups %}
            <div id="tab-specs" class="tab-pane fade{% if not description %} show active{% endif %}" role="tabpanel">
              <div class="pdp-tab-body">
                {% for attribute_group in attribute_groups %}
                  {% if attribute_group.name|lower|trim != text_pdp_characteristics|lower|trim %}
                    <h3 class="pdp-detail-subheading">{{ attribute_group.name }}</h3>
                  {% endif %}
                  <dl class="row pdp-dl mb-3">
                    {% for attribute in attribute_group.attribute %}
                      <dt class="col-sm-4 col-lg-3 pdp-dl__dt">{{ attribute.name }}</dt>
                      <dd class="col-sm-8 col-lg-9 pdp-dl__dd">{{ attribute.text }}</dd>
                    {% endfor %}
                  </dl>
                {% endfor %}
              </div>
            </div>
          {% endif %}
          <div id="tab-shipping" class="tab-pane fade" role="tabpanel">
            <div class="pdp-tab-body">
              <h3 class="pdp-detail-subheading">{{ text_pdp_shipping_heading }}</h3>
              <p>{{ text_pdp_shipping }}</p>
              <h3 class="pdp-detail-subheading">{{ text_pdp_guarantee_heading }}</h3>
              <p>{{ text_pdp_guarantee }}</p>
            </div>
          </div>
          {% if review_status %}
            <div id="tab-review" class="tab-pane fade" role="tabpanel">
              <div class="pdp-tab-body">{{ review }}</div>
            </div>
          {% endif %}
        </div>
      </div>
      {% if is_bundle and bundle_children %}
      <section class="bundle-pdp-children mt-4">
        <h3 class="bundle-pdp-children__heading">
          <i class="fa-solid fa-box-open"></i> {{ text_bundle_includes }}
          <span class="bundle-pdp-children__badge">-{{ bundle_savings_percent }}%</span>
        </h3>
        <div class="row row-cols-2 row-cols-sm-3 row-cols-md-4 row-cols-xl-6">
          {% for child in bundle_children %}
            <div class="col mb-3">{{ child }}</div>
          {% endfor %}
        </div>
      </section>
      {% endif %}
      {{ related }}
      {{ cross_sell }}
      {{ content_bottom }}
    </div>
    {{ column_right }}
  </div>
</div>
<script type="text/javascript"><!--
$('#input-subscription').on('change', function(e) {
    var element = this;

    $('.subscription').addClass('d-none');

    $('#subscription-description-' + $(element).val()).removeClass('d-none');
});

$('#form-product').on('submit', function(e) {
    e.preventDefault();

    $.ajax({
        url: 'index.php?route=checkout/cart.add&language={{ language }}',
        type: 'post',
        data: $('#form-product').serialize(),
        dataType: 'json',
        contentType: 'application/x-www-form-urlencoded',
        cache: false,
        processData: false,
        beforeSend: function() {
            $('#button-cart').button('loading');
        },
        complete: function() {
            $('#button-cart').button('reset');
        },
        success: function(json) {
            console.log(json);

            $('#form-product').find('.is-invalid').removeClass('is-invalid');
            $('#form-product').find('.invalid-feedback').removeClass('d-block');

            if (json['error']) {
                for (key in json['error']) {
                    $('#input-' + key.replaceAll('_', '-')).addClass('is-invalid').find('.form-control, .form-select, .form-check-input, .form-check-label').addClass('is-invalid');
                    $('#error-' + key.replaceAll('_', '-')).html(json['error'][key]).addClass('d-block');
                }
            }

            if (json['success']) {
                $('#alert').prepend('<div class="alert alert-success alert-dismissible"><i class="fa-solid fa-circle-check"></i> ' + json['success'] + ' <button type="button" class="btn-close" data-bs-dismiss="alert"></button></div>');

                $('#cart').load('index.php?route=common/cart.info&language={{ language }}');
            }
        },
        error: function(xhr, ajaxOptions, thrownError) {
            console.log(thrownError + "\r\n" + xhr.statusText + "\r\n" + xhr.responseText);
        }
    });
});

// Quantity change function
function changeQuantity(delta) {
    var $input = $('#input-quantity');
    var currentValue = parseInt($input.val()) || {{ minimum }};
    var newValue = currentValue + delta;
    var minimum = {{ minimum }};
    
    if (newValue < minimum) {
        newValue = minimum;
    }
    
    $input.val(newValue);
}

$(document).ready(function() {
    var $packGrid = $('.pdp-pack-choice-grid');
    var $packInput = $('#pdp-pack-option-input');
    if ($packGrid.length && $packInput.length) {
        var pdpSaleLabel = "{{ text_special_offer|e('js') }}";
        $packGrid.on('click', '.pdp-pack-choice-box', function() {
            var id = $(this).attr('data-pov-id');
            if (!id) {
                return;
            }
            $packInput.val(id);
            $packGrid.find('.pdp-pack-choice-box').removeClass('pdp-pack-choice-box--active');
            $(this).addClass('pdp-pack-choice-box--active');
            var $priceSlot = $('#pdp-main-price');
            if ($priceSlot.length) {
                var sale = $(this).attr('data-pdp-sale') || '';
                var was = $(this).attr('data-pdp-was') || '';
                if (was) {
                    $priceSlot.html(
                        '<li class="mb-1 product-price-block__row product-price-block__row--sale">' +
                        '<span class="product-price-sale-label">' + $('<div/>').text(pdpSaleLabel).html() + '</span> ' +
                        '<span class="product-price-sale price-display__sale">' + sale + '</span></li>' +
                        '<li class="mb-0 product-price-block__row product-price-block__row--was">' +
                        '<span class="product-price-was price-display__was">' + was + '</span></li>'
                    );
                } else {
                    $priceSlot.html(
                        '<li><h2 class="mb-0"><span class="product-price-regular">' + sale + '</span></h2></li>'
                    );
                }
            }
        });
    }

    $('.magnific-popup').magnificPopup({
        type: 'image',
        delegate: 'a',
        gallery: {
            enabled: true
        }
    });
    
    // Ensure quantity doesn't go below minimum
    $('#input-quantity').on('change blur', function() {
        var value = parseInt($(this).val()) || {{ minimum }};
        var minimum = {{ minimum }};
        if (value < minimum) {
            $(this).val(minimum);
        }
    });
});
//--></script>
{{ footer }}
